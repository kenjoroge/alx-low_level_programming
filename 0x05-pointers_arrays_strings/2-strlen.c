#include "main.h"
/**
 * _strlen - returns the length of a string
 *
 * @s: takes the argument for the string
 *
 * Return: string length
 */
int _strlen(char *s)
{
	int i;

	while (*s = '\0')
	{
		i++;
		s++;
	}
	return (i);
}
