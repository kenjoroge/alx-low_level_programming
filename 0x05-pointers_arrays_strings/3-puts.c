#include "main.h"
/**
 * _puts - prints a string followed by a new line
 *
 * @str: argument for string 
 *
 * Return: Always 0
 */
void _puts(char *str)
{
	char *str;
	_putchar(str);

	return (0);
}
