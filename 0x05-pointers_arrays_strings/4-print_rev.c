#include "main.h"
/**
 * print_rev - prints a string in reverse
 *
 * @s: Gives the argument for s
 *
 * Return: Always 0
 */
void print_rev(char *s)
{
	int j;

	while (*s[j] != '\0')
	{
		j++
	}
	
	j--;

	while (s[i] != '\0')
	{
		_putchar(s[j--]);
	}
	_putchar('\n');
}
