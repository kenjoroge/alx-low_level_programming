#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the programe name
 * @argc: Takes the counts of integers
 * @argv: Takes the array input of the programme
 *
 * Return: Always (0)
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
