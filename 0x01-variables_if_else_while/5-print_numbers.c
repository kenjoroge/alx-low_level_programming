#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print numbers fro zero to ten..
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char num;
	
	for (num = 0; num <= 9; num++)
	{
		printf("%d", num)
	}
	putchar('\n');
	return (0);
}

