#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two numbers
 *
 * @a: takes the first integer
 * @b: takes the second integer
 *
 * Return: Always 0
 */
int mul(int a, int b)
{
	return (a * b);
}
