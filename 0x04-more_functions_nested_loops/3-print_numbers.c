#include "main.h"
#include <stdio.h>
/**
 * print_numbers - prints numbers 1 to 9
 *
 * Returns: Always 0
 */
void print_numbers(void)
{
	int number;

	number = 48;

	for (number = 48; number <= 58; number++)
	{
		_putchar(number);

	}
	_putchar(10);
}
